name: CI

on:
  pull_request:
    branches:
      - main
      - develop
      - codex/merge-main-clss-rollout
  push:
    branches:
      - develop
      - codex/merge-main-clss-rollout
  workflow_dispatch:

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Run tests
        run: npm test -- --runInBand

  onboarding:
    name: onboarding
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Run onboarding gate
        run: npm run qa:onboarding
